import {
    BrowserRouter as Router,
    Routes,
    Route,
    Navigate,
} from "react-router-dom";

const appRoutes = () => {
    return (
        <Router>
            <Routes>
                <Router></Router>
            </Routes>
        </Router>
    )
}

export default appRoutes