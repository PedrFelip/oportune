import {
    BrowserRouter as Router,
    Routes,
    Route,
    Navigate,
} from "react-router-dom";

const appRoutes = () => {
    return (
        <Router>
            <Routes>
                <Route></Route>
            </Routes>
        </Router>
    )
}

export default appRoutes